pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "io.quarkus" version "${quarkusVersion}"
    }
}

include('auto-maptive-core')
include('auto-maptive-client-retrofit')
//include('auto-maptive-app')
